#include "truthMaker.h"

/*
namespace module {
    TruthMaker::update() {


        float V_l = V * duty_l;
        
        float V_f = V_l + V_r;
        float V_r = - V_l + V_r;
        
        a_y = (V_f - coef_v * v_y - coef_c)/ coef_a;
        v_y = v_y + a * DELTA_T;

        ang_a = (V_r - coef_r_v * ang_v - coef_r_c)/ coef_r_a;
        ang_v = ang_v + ang_a + DELTA_T;
        ang   = ang + ang_v * DLETA_T;
        ang_rad = deg2rad(ang);

        beta =  

        v_y = v * 
        v_x = v *

        x_d = v_y * cosf(ang_rad) + v_x * sinf(ang_rad);
        y_d = v_y * sinf(ang_rad) - v_x * cosf(ang_rad);
        x = x + x_d * DELTA_T;
        y = y + y_d * DELTA_T;

        


    }

}
*/
